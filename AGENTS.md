# 《电梯求生》项目协作规则

1. 项目名称为《电梯求生》。
2. 本项目是一个网页单人策略生存 Roguelite 游戏。
3. 开始任何开发任务前，必须先阅读：
   - `docs/01-game-design-v0.1.md`
   - `docs/03-architecture.md`
   - 与当前任务相关的 `docs/content/` 文档
4. 已确认的技术方向为：
   - React
   - TypeScript
   - Vite
   - Zustand
   - Zod
   - Vitest
5. 核心游戏规则必须使用纯 TypeScript 实现，不得依赖 React。
6. React 只负责展示状态和发送命令，不能在组件中直接实现核心结算规则。
7. 游戏随机行为必须通过统一、可注入、支持种子的随机数生成器实现，不得在业务逻辑中零散使用 `Math.random()`。
8. 内容应尽量数据驱动，并通过 Schema 校验。
9. 修改已经确定的产品或架构规则前，不得自行决定；应先说明冲突，并等待确认。
10. 新增或修改核心规则时，必须同步更新相关文档和测试。
11. 当前阶段禁止擅自扩展首版范围，例如多人联机、交易系统、复杂家园、战术格子地图、随机装备词条等。
12. 执行任务后必须报告：
    - 修改了哪些文件；
    - 做了什么；
    - 是否执行了测试或检查；
    - 有哪些尚未解决的问题。
