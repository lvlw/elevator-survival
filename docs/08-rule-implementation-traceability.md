# 规则实现追踪矩阵

| 场景非战斗手电筒电池充能 | DEC-036 | `scene.batteryUseTime`、`maintenance.flashlightCharge` 与医院电池→手电筒中性兼容目录 | `src/core/device-recharge/`、`src/core/scene-exploration/` | `src/content/hospital-v0.1/scene-battery.integration.test.ts` | 已实现 | 仅允许真实背包通用电池为背包或实用装备位中的未充满手电筒充能；恢复、行动时间、流血、死亡和超时返程均经单一冻结 Scene Effect 计划结算，不实现战斗充能或自动选择。 |

> 本文件用于连接设计决策、版本化配置、实现和测试，不是权威规则来源。实际规则以已确认 DEC 为准；存在类型、接口或测试辅助组合不代表完整玩法命令链已经实现。

| 主题 | 相关 DEC | 配置路径 | 实现路径 | 主要测试路径 | 状态 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 版本化规则配置与 Run 绑定 | DEC-009、DEC-014 | `src/content/hospital-v0.1/rule-config.ts` | `src/core/config/`、`src/core/domain/run-identity.ts`、`src/content/rule-config-registry.ts` | `src/content/rule-config-registry.test.ts`、`src/core/domain/run-identity.test.ts` | 已实现 | 已实现最小 Run 身份与配置版本绑定，不是完整 RunState。 |
| 唯一 Run 存档与稳定边界恢复 | DEC-009、DEC-014、DEC-027、DEC-028 | `metadata.rulesVersion`、`saveFormatVersion = 1` | `src/state/run-save/`、`src/state/command-execution/` | `src/state/run-save/run-save.integration.test.ts` | 已实现最小边界 | 已实现单槽、当前日中枢／Scene Session／Run失败三种互斥稳定阶段、独立格式版本、规则版本注册表与 core 严格恢复。正式状态变更 executor 先规范化输入，拒绝终止阶段，在 handler 完整提交后使用同一入口规范化输出并验证 RunIdentity 连续性，随后通过唯一 Run Save 写入一次；规则拒绝、Effect不匹配和跨Run输出不写入，存储失败不回滚已提交的内存结果。Success 仍是 DEC-028 定义的未来正式终局概念；当前规则版本没有 Success Resolver 或主线条件，不能构造、保存或恢复 `run-success`。已实现无状态 Headless Application 统一分派、最小 vanilla Store 与只读 React 展示桥接；未实现 Profile、完整RunState、交互式 UI 编排、放弃、New Run、存档迁移或多槽历史。 |
| Stable Run 统一 Application 分派与确定性重放验收 | DEC-006、DEC-009、DEC-010、DEC-011、DEC-014、DEC-024、DEC-025、DEC-027、DEC-028、DEC-032、DEC-035、DEC-037、DEC-040 | 复用 canonical phase 的 `RunIdentity.rulesVersion`、Run seed 与既有 Run Save registry | `src/state/run-application/`，委托 `src/state/run-lifecycle/`、`src/state/run-scene/`、`src/state/run-hub/` | `src/state/run-application/run-application.integration.test.ts` | 已实现 Headless facade | 统一入口只严格分类并冻结 lifecycle／scene／hub application command，内部命令复用三个 specialized parser；dispatcher 不复制 phase matrix、玩法、generic executor 或保存路径，每次只委托一个 specialized router。双独立 storage、每步 reload 对照及完整医院链验证相同 seed、规则版本与有序命令产生相同 result、canonical phase、稳定实例身份与序列化存档；Search、Obstacle 与 Combat 的命名随机子流不因统一分派或 reload 改变。Replay 仅用于测试，不持久化 command history。只读 React 展示已实现；制作、拆解与 React gameplay command wiring 尚未实现。 |
| 最小 Stable Run Application Store | DEC-006、DEC-009、DEC-014、DEC-024、DEC-027、DEC-028 | canonical `StableRunPhase`、既有 Run Save registry 与 storage | `src/state/run-store/`，只调用 `src/state/run-application/` | `src/state/run-store/run-store.integration.test.ts`、`src/ui/**/*.test.ts` | 已实现 Headless Store 与只读 React bridge | 使用 `zustand/vanilla` 建立一个前台 Run session 的唯一长期 phase owner；snapshot 只含 canonical `StableRunPhase`，公开 API 不含 `setState`。初始化复用 strict canonicalization，storage bootstrap 复用 strict load；dispatch 只经统一 Application facade。成功与存储失败各通知一次并采用 committed `execution.phase`，规则拒绝不更新；失败后下一命令从内存 committed phase 继续，不回滚、reload 或 retry。Execution/result/Effects 不进入 Store state；React 仅通过公开 `getState`、`getInitialState` 与 `subscribe` 建立只读展示订阅，不在渲染路径 dispatch、保存或拥有第二份状态。 |
| React 只读展示桥接 | AGENTS.md、DEC-009、DEC-014、DEC-027、DEC-028、DEC-031、DEC-034 | canonical `StableRunPhase` 与既有玩家可见 core 查询 | `src/ui/` | `src/ui/**/*.test.ts` | 已实现最小展示层 | Hub、Scene、Combat 与 Failure 使用纯 TypeScript 白名单 ViewModel 投影；Scene 导航只显示当前可通行相邻节点，不宣称完整玩家已知地图。开发检查器与独立内存态预览只在开发环境显示且没有 mutation；预览通过正式构造器创建合法独立 Store，选择示例不发送 gameplay command 或保存。New Run、真实 UI 命令、完整应用编排与终版视觉仍未实现。 |
| Stable Run 生命周期命令路由 | DEC-003、DEC-009、DEC-014、DEC-027、DEC-028 | 复用 stable phase 的 `RunIdentity.rulesVersion` 与现有 Run Save registry | `src/state/run-lifecycle/`、`src/state/command-execution/` | `src/state/run-lifecycle/run-lifecycle.integration.test.ts` | 已实现最小路由 | 严格路由主要场景启动、终止Scene结算和结束本日；Hub启动调用正式Scene Launch，安全／强制返回调用正式Run Return，死亡Scene和日结算终止调用正式RunFailure，普通日结算生成次日Hub。输入 canonicalization 复用 Scene strict restore：伪造的非安全节点 returned Scene、正时间 forced return、死亡 returned 玩家，或超出当前 `rulesVersion` 的 `scene.totalTime` 的 `remainingTime` 会在 resolver 与保存前拒绝且不修复；Router不复制位置或时间上限规则，也不直接保存。成功结果只经generic executor写入一次，`execution.phase`是下一命令的唯一状态。最小 Store 已通过统一 facade 接入；React gameplay command UI 编排、Success、放弃与New Run仍未实现。 |
| 基础 Stable Run Scene mutation routing | DEC-003、DEC-006、DEC-009、DEC-014、DEC-015、DEC-016、DEC-017、DEC-023、DEC-024、DEC-025、DEC-027、DEC-029、DEC-030、DEC-031、DEC-032、DEC-034、DEC-035、DEC-036、DEC-037、DEC-040 | 复用 canonical Session 的 `RunIdentity.rulesVersion`、Run Save registry 与正式 Scene runtime bundle | `src/state/run-scene/`、`src/state/command-execution/`、既有 Scene core resolvers | `src/state/run-scene/run-scene.integration.test.ts`、`src/state/run-scene/run-scene-combat.integration.test.ts`、`src/core/scene-exploration/scene-interaction-command-normalization.test.ts` | 已实现基础路由 | 严格路由移动、主要搜索、节点物品拾取、七种 Scene 背包／快捷栏整理、主动撤离、障碍、任务事件、场景医疗、设备充能与战斗玩家行动。Combat 结构、CTB、敌人行动、确定性随机、资源消费、逃跑和终局仍由 Combat core 与 Scene combat integration 拥有；Router 不拥有玩法、随机数或直接保存。每次经 generic executor 提交一个 canonical Scene Session，规则拒绝不保存，写入失败不重跑 resolver。ongoing Combat、胜利、逃跑与战败均先保存 `scene-session`，terminal Scene 后续必须显式执行生命周期结算；`execution.phase` 是唯一下一状态。最小 Store 已通过统一 facade 接入；战斗换装、完整战斗背包整理与对应 React gameplay command UI 尚未接入。 |
| 基础 Stable Run Hub mutation routing | DEC-006、DEC-007、DEC-009、DEC-014、DEC-016、DEC-017、DEC-021、DEC-027、DEC-033、DEC-034、DEC-036、DEC-039 | 复用 canonical Hub 的 `RunIdentity.rulesVersion`、Run Save registry、正式 CurrentDayHub 与 Hub Maintenance 依赖 | `src/state/run-hub/`、`src/state/command-execution/`、既有 Hub core resolvers | `src/state/run-hub/run-hub.integration.test.ts` | 已实现基础路由 | 严格路由十类 Run loadout、Hub medical、两类 Hub survival 与五类 Hub maintenance 命令。Router 不拥有容器、医疗、资源、生存或维护规则，不直接保存；同版本 registry 验证 Hub Maintenance 与 CurrentDayHub 依赖同源。每次只提交一个 canonical CurrentDayHub，规则拒绝不保存，存储失败不重跑或双重消费；`execution.phase` 是唯一下一状态。Hub mutation 不推进日期，End Day 仍由独立 lifecycle 命令执行。最小 Store 已通过统一 facade 接入；制作、拆解与对应 React gameplay command UI 尚未实现。 |
| 确定性随机 | DEC-010 | 随机算法版本常量 | `src/core/random/` | `src/core/random/*.test.ts` | 已实现 | 命名子流和黄金输出已锁定；尚未接入完整内容生成。 |
| 场景时间与超时债务 | DEC-024、DEC-030、DEC-035 | `scene.*` | `src/core/scene/timed-scene-action.ts` | `src/core/scene/timed-scene-action.test.ts` | 已实现 | 实现通用事务，不代表全部场景行动已实现。 |
| 强制返程伤害 | DEC-033、DEC-035 | `forcedReturn.*` | `src/core/scene/forced-return.ts` | `src/core/scene/timed-scene-action.test.ts` | 已实现 | 与行动后流血使用独立配置。 |
| 负载档位 | DEC-015、DEC-016、DEC-030 | `backpack.weightBands` | `src/core/load/load-tier.ts` | `src/core/load/load-tier.test.ts` | 已实现 | 严格只接收背包重量小计。 |
| 移动与返程倍率 | DEC-033、DEC-038 | `scene.travelTimeModifiers`、`backpack.weightBands` | `src/core/load/travel-time.ts` | `src/core/load/travel-time.test.ts` | 已实现 | 倍率相乘后统一向上取整。 |
| 场景节点图与确定性寻路 | DEC-004、DEC-029、DEC-030 | 医院版本化图定义 | `src/core/scene-graph/`、`src/content/hospital-v0.1/hospital-scene-graph.ts` | `src/core/scene-graph/*.test.ts` | 已实现 | 实现静态图、通行状态和返程估算，不含完整节点交互。 |
| 物品物理目录 | DEC-017、DEC-029 | 医院物品定义 | `src/core/inventory/item-catalog.ts`、`src/content/hospital-v0.1/items/` | `hospital-item-catalog.test.ts` | 已实现 | 覆盖医院18项物品物理属性。 |
| 背包几何与重量小计 | DEC-015、DEC-016、DEC-017 | `backpack.*` | `src/core/inventory/` | `src/core/inventory/*.test.ts` | 已实现 | 仅计算背包负重，不创建角色总负重。 |
| 耐久、完整度与电量 | DEC-018、DEC-019、DEC-020、DEC-036 | `combat.metalPipe.maxDurability`、`maintenance.*` | `src/core/item-state/`、医院资源目录 | `src/core/item-state/item-state.test.ts`、医院资源测试 | 已实现 | 实现状态资源基础和通用上限恢复；具体中枢维护与充能行为见下项。 |
| 装备资格与背包转移 | DEC-007、DEC-016、DEC-020、DEC-037 | 医院装备资格目录 | `src/core/equipment/`、医院装备目录 | `src/core/equipment/equipment.test.ts`、医院装备集成测试 | 已实现 | 装备不占格且不计入背包负重；上下文换装资格未实现。 |
| 玩家生命、流血、伤口、挫伤、暴露与镇痛 | DEC-026、DEC-031、DEC-034、DEC-035 | `combat.player`、`combat.escape`、`medical.*`、`scene.postActionBleedingDamage` | `src/core/condition/`、`src/core/daily-state/`、`src/core/medical/`、`src/core/combat/`、`src/core/scene-exploration/`、`src/core/run-hub-medical/` | `src/core/condition/condition.test.ts`、医院场景／战斗／中枢医疗集成测试 | 部分实现 | 已实现类型化伤口、待处理感染暴露、日级消毒剂次数，以及场景／中枢绷带、止痛药、消毒剂、急救包和战斗绷带／止痛药；中枢为零时间库存、条件与日级使用事务。场景／中枢共享医疗内容绑定的生命周期严格校验；战斗医疗在其内容依赖校验中读取同一正式生命周期目录。医院感染进展、原子日结算和最小失败终止协调已实现；已实现的医疗命令已通过 Stable Run application／Store／generic executor 接入命令后正式保存路径，React 已可只读展示状态，交互式医疗命令 UI 尚未实现。 |
| 消防斧耐久2 | DEC-039 | `maintenance.itemResourceMaximums.fireAxeDurability` | 医院物品资源目录 | `hospital-item-resource-profiles.test.ts` | 已实现 | 只追踪资源状态，不代表消防斧攻击或维修配方已实现。 |
| 当前日中枢装备维护与手电筒充能 | DEC-006、DEC-007、DEC-016、DEC-018、DEC-019、DEC-020、DEC-036、DEC-039 | `maintenance.dailyBaseLabor`、`maintenance.materialRepair`、`maintenance.toolkitRepair`、`maintenance.flashlightCharge`、医院维护 profile／材料绑定 | `src/core/hub-maintenance/`、`src/core/hub-inventory/`、`src/core/item-state/`、`src/content/hospital-v0.1/items/hospital-item-maintenance-*.ts` | `src/content/hospital-v0.1/hub-maintenance.integration.test.ts` | 已实现 | 在合法 CurrentDayHub 上实现基础工时、金属零件、布料、工具箱双材料和手电筒电池充能；目标为真实仓库／背包／装备栏实例，材料来源显式且原子消费，计划／预览／应用共用精确 Effect 比较。消防斧无维护 profile；制作、拆解、完整 RunState 与对应 React gameplay command UI 未实现。 |
| 统一 Effect 管线 | DEC-006 | 复用场景、负载、战斗与返程配置 | `src/core/scene-exploration/` | 核心场景Effect、搜索、拾取、障碍与场景战斗命令测试，医院集成测试 | 部分实现 | 场景移动、主要搜索、节点物品拾取、防火门和场景战斗已采用冻结Effect计划和唯一应用器生成最终快照；战斗推进内嵌并验证完整Combat计划；尚未形成通用命令总线。 |
| 场景移动命令 | DEC-004、DEC-024、DEC-030、DEC-033、DEC-035、DEC-038 | `scene.*`、`forcedReturn.*`、`backpack.weightBands` | `src/core/scene-exploration/` | 核心移动命令与Effect防篡改测试、`src/content/hospital-v0.1/scene-movement-command.integration.test.ts` | 已实现 | 严格校验单字段边ID命令；移动、负载、挫伤、返程、流血、终局与战斗触发共用唯一完整Effect计划，应用前会重新生成并完整比对；不是完整 RunState，医院切片尚不可玩。 |
| Scene 非战斗背包与快捷栏整理 | DEC-007、DEC-016、DEC-017、DEC-021、DEC-023、DEC-024、DEC-035、DEC-040 | 背包几何、快捷栏资格、物品生命周期及稳定拆分身份 | `src/core/inventory/`、`src/core/scene-exploration/scene-inventory-command.ts` | 背包、节点拾取与场景探索回归测试 | 已实现最小边界 | active 且存活的 Scene 支持零时间背包移动／旋转、显式拆分与合并、背包和快捷栏单件转移，以及普通、权限和经确认任务物的节点暂存；实例和唯一 ItemState 随真实容器移动。正式Effect应用从初始快照和调用方独立提供的规范化命令重建完整计划，Effect内嵌命令、审计与快照不能自行授权另一条合法操作。快捷栏使用后不会自动补充；撤离继续从整理后的真实背包、伤势和权限状态即时派生。 |
| Scene 主动撤离／正式返程命令 | DEC-008、DEC-024、DEC-027、DEC-033、DEC-035、DEC-038、DEC-040 | 复用医院节点图、边权限、`scene.*`、`forcedReturn.*` 与负载倍率配置 | `src/core/scene-exploration/scene-withdrawal-*`、`src/core/scene-launch/` | `src/content/hospital-v0.1/scene-launch.integration.test.ts` | 已实现最小边界 | 非战斗 active Scene 仅在存在已知可通行返程路线时可撤离；路线和耗时由共享寻路／负载／挫伤／镇痛规则派生。正时间撤离按流血、死亡和超时强制返程结算；安全节点允许零距离确认。撤离只生成 terminal Scene Session，后续物品提取仍由 Run Return 单独处理。 |
| 场景节点主要搜索 | DEC-010、DEC-013、DEC-023、DEC-024、DEC-030、DEC-032、DEC-035 | `scene.searchTime`、医院搜索与照明资格目录 | `src/core/scene-search/`、`src/core/scene-exploration/`、`src/content/hospital-v0.1/search/` | 核心搜索及命令测试、医院搜索命令集成测试 | 部分实现 | 已实现结果预定、搜索命令、照明和揭示；独立互动及搜索 React gameplay command UI 尚未实现。 |
| Run 情报日志 | DEC-013、DEC-023、DEC-032 | 无独立内容配置 | `src/core/run-intel/`、`src/core/scene-exploration/` | 搜索、任务事件与严格快照恢复测试 | 部分实现 | 已实现最小 Run 级已知情报集合；搜索状态只记录节点揭示历史；严格恢复保证已揭示情报不会从 RunIntel 消失。 |
| 场景任务事件与医院样本箱 | DEC-006、DEC-023、DEC-029、DEC-032、DEC-035 | 医院任务事件目录、`scene.extractionTime`、`scene.pathogenCaseRetrieval` | `src/core/scene-task-event/`、`src/core/scene-exploration/`、`src/content/hospital-v0.1/task-events/` | `pathogen-case-retrieval.integration.test.ts` | 已实现场景取得 | 已实现严格任务事件目录、状态、前置遭遇绑定、确定性污染、稳定任务物实例和完整 Effect 回放；场景事件完成只表示取得动作完成，安全返回后的任务完成由 Run 任务储存区数量派生。 |
| 安全返回结算与最小 Run 库存 | DEC-007、DEC-014、DEC-023、DEC-027、DEC-035 | 医院物品返回生命周期目录 | `src/core/run-return/`、`src/content/hospital-v0.1/items/hospital-item-return-lifecycle-catalog.ts` | `src/content/hospital-v0.1/run-return.integration.test.ts` | 已实现最小边界 | 生还的安全／强制返回将背包普通及权限物品转入 Run 仓库、任务物品转入任务储存区；保持装备、快捷栏、ItemState、RunIntel和DailyUsage，并通过返回记录与完整 Effect 回放防止重复结算。Return 本身不执行中枢操作或日结算；终止 Scene 的返回结算应用命令及命令后正式保存路径已接入，完整 RunState、Profile 与交互式 React UI 尚未实现。 |
| Run 最小整备库存 | DEC-007、DEC-015、DEC-016、DEC-017、DEC-023 | `backpack.*`、医院物理／资源／装备／快捷栏／返回生命周期目录 | `src/core/run-loadout/`、`src/core/run-hub-medical/`、`src/core/hub-maintenance/` | `src/content/hospital-v0.1/run-loadout.integration.test.ts`、`run-hub-medical.integration.test.ts`、`hub-maintenance.integration.test.ts` | 已实现最小边界 | 已实现从返回快照进入整备库存、仓库↔背包、背包↔装备、背包↔快捷栏及快捷栏内部整理；任务储存区只读。中枢医疗和中枢维护复用真实库存与唯一 ItemState，无自动整理、合并或补充。本库存模块本身不执行日结算、完整 RunState 或对应 React gameplay command UI；制作与拆解尚未实现。 |
| 当前日中枢、生存事实与生存消耗品 | DEC-003、DEC-009、DEC-014、DEC-027、DEC-028、DEC-033、DEC-034、DEC-036、DEC-039 | `metadata.rulesVersion`、`worldThreat.*`、`dailySettlement.*`、`maintenance.*`、医院生存／维护物品绑定 | `src/core/domain/run-phase-continuity.ts`、`src/core/run-return/`、`src/core/run-loadout/`、`src/core/current-day-hub/`、`src/core/hub-maintenance/`、`src/core/world-threat/`、`src/core/satiety/`、`src/core/daily-state/`、`src/core/daily-settlement/`、`src/core/hub-inventory/` | `src/core/domain/run-phase-continuity.test.ts`、`src/content/hospital-v0.1/run-return.integration.test.ts`、`run-loadout.integration.test.ts`、`current-day-hub.integration.test.ts`、`daily-settlement.integration.test.ts`、`hub-maintenance.integration.test.ts` | 已实现当前日与日结算边界 | Return输入只接受由CurrentDayHub或RunReturn稳定聚合正式投影的carry-forward；该聚合包含continuity、普通与任务库存、对应ItemState及返回记录，调用方不能独立拼接既有Run事实。活动Hub拒绝终末威胁。结束本日通过唯一冻结Effect计划结算持续危险、威胁、暴露、饱食、匮乏、恢复、轻伤生命周期、日级重置和日期推进；终止结果不恢复成活动Hub。中枢维护在同一严格Hub快照上执行，维护后的资源状态保留并由既有日结算在成功时重置当日工时；完整七日世界推进、完整RunState和对应 React gameplay command UI尚未实现。 |
| 每日主要场景使用与正式 Scene 生命周期 | DEC-003、DEC-009、DEC-010、DEC-014、DEC-023、DEC-027、DEC-028 | `metadata.rulesVersion`、医院完整 Scene runtime bundle | `src/core/daily-state/`、`src/core/scene-launch/`、`src/core/run-return/`、`src/core/run-termination/`、`src/content/hospital-v0.1/hospital-scene-runtime.ts` | `src/core/daily-state/daily-run-state.test.ts`、`src/core/scene-exploration/scene-exploration.test.ts`、`src/content/hospital-v0.1/scene-launch.integration.test.ts`、`daily-settlement.integration.test.ts`、`run-termination.integration.test.ts` | 已实现最小生命周期 | `DailyRunState.mainSceneUsedToday` 是唯一日级 owner；初始／成功次日为 false，正式启动、主动／强制返回后为 true，终局不执行普通重置。Scene ID 由 Run 身份、规则版本、当前日和正式内容确定性派生。医院 Session 只能由包含 combat、医疗生命周期和设备充能在内的完整 runtime 恢复；strict Scene restore拒绝非返程安全节点的active零时间状态，并要求safe／forced returned位于正式返程安全节点、玩家存活，其中forced returned必须为零剩余时间；dead只要求生命为0，不限制节点。Run Save、Return、lifecycle settlement与Scene death termination共用该严格入口和同一Session context provenance。Scene 生命周期应用命令及命令后正式保存路径已接入；完整 RunState、React gameplay command UI 与 Day 7 最终解析器尚未实现。 |
| 节点物品拾取 | DEC-006、DEC-007、DEC-015、DEC-016、DEC-017、DEC-023、DEC-024、DEC-032 | 医院物理物品、资源档案与搜索定义 | `src/core/scene-search/`、`src/core/scene-exploration/node-item-pickup-command.ts` | 核心拾取命令测试、医院拾取集成测试 | 已实现最小边界 | 已完成显式拾取、部分数量的核心派生身份、按稳定摆放顺序补满多个兼容堆叠并物化剩余堆叠，以及背包摆放、负载和单一原子 Effect 提交；调用方不能提供部分拾取实例 ID。正式Effect应用以调用方独立提供的规范化拾取命令重建数量、摆放、transfer与拆分身份，Effect不能通过协同篡改为另一条合法拾取自行授权。任务物经显式确认丢在节点后会由正式撤离与 Run Return 记录为场景遗失，对应 React gameplay command UI尚未实现。 |
| 医院防火门 | DEC-029、DEC-030、DEC-032、DEC-035、DEC-036、DEC-040 | `scene.fireDoor`、医院障碍与物品目录 | `src/core/scene-obstacle/`、`src/core/scene-exploration/`、医院障碍目录 | 障碍目录、Effect防篡改、医院防火门与完整流程集成测试 | 已实现 | 六种选项共用唯一主要结果计划；正式时间、资源、警觉、风险、挫伤和产物均按完整前缀回放验证；障碍内容目录是严格运行时边界；对应 React gameplay command UI尚未实现。 |
| 工作人员通道 | DEC-029、DEC-030、DEC-040 | 医院节点图、边权限和权限物品目录 | `src/core/scene-access/`、医院边权限目录 | 医院工作人员通道真实流程测试 | 已实现 | 背包持卡实时授权，不消耗卡且无额外开门行动；权限边不写入 `enabledEdgeIds`。 |
| 工具箱开门产物 | DEC-032、DEC-036、DEC-040 | 医院事件与物品目录 | `src/core/scene-items/`、`scene-item-spawned` Effect | 医院工具箱开门、Effect防篡改与拾取流程测试 | 已实现 | 电子元件以稳定实例ID进入当前节点地面状态，不自动拾取或合并；应用器严格验证产物数量、顺序、身份和初始状态。 |
| 场景单级警觉 | DEC-032 | 医院防火门内容、`combat.infectedOrderly.firstActionTime` | 场景探索警觉状态与战斗首次遭遇入口 | 医院防火门、战斗时间轴与场景遭遇测试 | 已实现 | `unalerted/alerted` 决定隔离走廊首次遭遇行动70/50；逃跑重入固定为50，警觉不直接修改伤害或意图。 |
| 场景战斗遭遇 | DEC-006、DEC-029、DEC-031、DEC-035 | 医院遭遇目录、`combat.sceneTimeConversion` | `src/core/scene-combat/`、`src/core/scene-exploration/` | 场景遭遇核心测试、医院进入／胜利／逃跑／重入／超时及严格恢复集成测试 | 已实现 | 已记录来源节点与真实入口边，严格恢复会复核当前通行授权、战斗正时间和敌人节点不变量；隐藏状态字段不可通过省略而重置，终局仍只写回一次；不是完整RunState。 |
| 快捷栏 | DEC-007、DEC-016、DEC-034 | `backpack.quickSlotCount`、`medical.*`、医院快捷栏资格与医疗绑定 | `src/core/quick-slot/`、`src/core/combat/`、`src/core/scene-exploration/`、`src/core/run-hub-medical/`、医院物品内容目录 | 核心快捷栏、命令严格性和ItemState移除测试，医院场景、战斗及中枢医疗集成测试 | 已实现医疗物品使用 | 已实现实际单件实例、堆叠抽取、放回、槽间转移，及场景／中枢绷带、止痛药、消毒剂、急救包和战斗绷带／止痛药的显式具体命令、原子消耗与不自动补充；快捷栏 React gameplay command UI尚未实现。 |
| 感染护工单敌人CTB战斗 | DEC-006、DEC-026、DEC-031、DEC-032、DEC-034、DEC-035、DEC-036、DEC-037 | `combat.*`、`medical.bandage`、`medical.painkiller` | `src/core/combat/`、`src/core/scene-combat/`、`src/content/hospital-v0.1/combat/` | 核心敌人目录、命令与换算测试，医院战斗医疗及场景遭遇集成测试 | 部分实现 | 已完成严格恢复、敌人循环、运行时内容绑定、连续CTB Effect链、既有五种玩家行动、快捷栏绷带／止痛药、逃跑锁定与同点优先、节点自动触发、持久重入、终局场景时间和强制返程写回；战利品、多目标及其他医疗上下文尚未实现。 |
| 日结算、存档与完整 Run 编排 | DEC-009、DEC-025、DEC-027、DEC-028、DEC-033、DEC-034、DEC-036 | `dailySettlement.*`、`worldThreat.stages[*].dailyRecoveryModifier` | `src/core/daily-settlement/`、`src/state/run-save/` | `src/content/hospital-v0.1/daily-settlement.integration.test.ts`、`src/state/run-save/run-save.integration.test.ts` | 已实现原子日结算与稳定存档边界 | 已实现严格结束本日命令、同源预览、完整Effect防篡改、三类提前终止、次日中枢生成及第七日终局协调边界；失败终止由上层协调器消费，已提交的次日中枢可严格存取。结束本日应用命令及命令后正式保存路径已通过 lifecycle router、统一 application facade、Store 与 generic executor 接入；正式七日世界推进与完整RunState仍未实现。 |
| Run 失败终止协调 | DEC-009、DEC-014、DEC-023、DEC-027、DEC-028、DEC-033、DEC-035 | 复用绑定规则版本与既有终止来源 | `src/core/run-termination/`、`src/core/scene-launch/`、`src/core/run-return/scene-storage-identity.ts`、`src/state/run-save/` | `src/content/hospital-v0.1/run-termination.integration.test.ts`、`scene-launch.integration.test.ts`、`src/state/run-save/run-save.integration.test.ts` | 已实现最小失败边界与稳定存取 | 已将生命归零的死亡Scene、日结算生命归零及世界威胁终末映射为严格、冻结的RunFailureSnapshot；Scene死亡直接消费正式Run Scene Session及其共享context，不再从旧CurrentDayHub独立投影Scene provenance。终局Scene经过完整runtime严格恢复，全局物理实例冲突与完整Effect计划会被校验。失败快照可作为终止阶段严格存取，并按正式规则重新生成派生摘要；终止协调器本身不直接写存档，生命周期 application／Store／generic executor 负责已接入命令的正式保存。协调器仍不执行死亡提取、日期推进或Profile写入；Day7终局、成功、放弃、期限／剧情失败、新Run、完整RunState和React gameplay command UI仍未实现。 |
