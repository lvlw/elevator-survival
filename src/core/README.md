# Core

纯 TypeScript 规则、状态转移、Effect、确定性随机与结算逻辑的边界。

- 配置契约和运行时校验属于核心，具体医院配置不属于核心。
- 最小 Run 身份在创建时绑定已注册的规则版本。
- 核心不依赖 React、React DOM、Zustand、DOM 或浏览器 API。
- 场景边移动、主要搜索、节点物品显式拾取、医院防火门、工作人员通道实时权限、单级警觉、感染护工战斗、医院样本箱任务事件、返回结算、最小整备库存管理，以及场景、战斗与电梯中枢医疗已经实现；场景和中枢医疗支持绷带、止痛药、消毒剂和急救包，战斗医疗支持绷带和止痛药。维修、制作、拆解、充电、日结算和完整 RunState 尚未实现。

## 确定性随机

`src/core/random` 提供版本化的计数器式确定性随机基础。结果由种子、随机算法版本、命名子流和抽取序号共同决定，各子流彼此隔离。

- 禁止使用 `Math` 对象的 `random()` 或任何时间、系统随机源。
- 算法输出由黄金测试锁定。
- 改变既有随机序列必须增加算法版本，不能静默修改旧版本。
- 规则版本决定游戏参数；随机算法版本决定种子与抽取序号如何映射为随机结果。

## 场景节点搜索状态

`src/core/scene-search` 负责主要搜索定义、独立随机物化、场景级预定结果和节点揭示状态。

- 搜索结果只依据 Run seed、场景实例稳定ID、节点稳定ID、主要搜索序号和随机用途标识独立物化。
- 场景搜索状态创建时即为所有可搜索节点预定结果；节点搜索顺序及其他随机子流消耗不改变结果。
- 每个节点物品由物理实例与身份一致的资源状态共同组成；搜索定义必须显式声明初始资源状态，不能按名称推断或默认填满。
- 搜索前的预定结果属于隐藏状态；搜索揭示会把物品移入独立的节点地面物品状态，搜索与事件产物共用同一地面状态和显式拾取系统。
- 玩家可见投影在搜索前只公开“可搜索”，不得返回预定物品、情报、随机命中或实例ID。
- `preparedOutcome` 属于场景真实状态，React 不得直接将其作为玩家情报展示。
- `validateSceneSearchState` 是场景真实搜索状态的严格恢复校验边界；玩家 UI 仍必须使用玩家可见投影，不能直接读取隐藏预定结果。
- SceneSearchState 只记录节点曾揭示过哪些情报；RunIntelLog 才是当前 Run 已知情报的唯一集合。严格场景恢复会保证已揭示的情报不会从 RunIntelLog 消失，但不会要求 RunIntelLog 的所有情报都来自当前场景搜索。
- 本模块不执行背包放置或堆叠合并；拾取命令由场景探索模块编排。

## Run 返回结算

`src/core/run-return` 负责把生还的场景终局转换为稳定的当前 Run 返回结果。

- 只有 `safe-returned` 与 `forced-returned` 可以结算；死亡、战斗中和仍在探索的场景不能入库。
- 严格生命周期目录决定背包物品的去向：普通及权限物品进入 Run 普通仓库，任务物品进入 Run 任务储存区。
- 结算只迁移背包物品；装备栏与快捷栏保持原实例和位置，背包清空但尺寸保持不变。
- 一个统一 ItemState 集合覆盖结算后装备、快捷栏、普通仓库和任务储存区中的全部 Run 所有物，迁移不恢复、删除或复制资源状态。
- RunIntelLog 与 DailyMedicalUsage 直接使用终局场景的最终值；返回不推进日期，也不重置每日消毒剂次数。
- 返回记录按场景实例防止重复结算；任务进度只从任务储存区数量派生，不把场景任务事件完成状态当作主线完成事实。
- 返回计划通过完整冻结 Effect 数组重建和比对后应用；节点遗留物、未拾取搜索物和未携带任务物不会自动入库。
- 当前未实现日结算、完整 RunState、Profile 收藏、存档和 UI。

## Run 最小整备库存

`src/core/run-loadout` 负责安全返回后的主动库存整理，不是完整 RunState 或中枢玩法实现。

- `RunLoadoutSnapshot` 统一校验普通仓库、只读任务储存区、背包、装备栏、快捷栏及其唯一 ItemState；所有真实实例跨容器唯一。
- 只提供玩家显式选择的仓库↔背包、背包整理、背包↔装备、背包↔快捷栏和快捷栏内部操作；不会自动找位置、旋转、合并、拆分或补充。
- 任务储存区没有进入任何整备命令，样本箱不会被重新取出；普通物品也不能转入任务储存区。
- 所有使物品进入背包的操作都使用正式背包负重并拒绝 `cannot-carry`；装备和快捷栏本身不计入背包负重。
- 堆叠物转入快捷栏时由核心从来源实例ID及拆分前数量确定性派生新ID；仅允许 `resource:none` 的堆叠拆分，原实例状态不恢复或复制有限资源。
- 每项命令生成冻结的完整 Effect 计划；应用前重新生成并完整比较，拒绝容器、实例、数量、摆放、槽位、拆分ID、ItemState和顺序篡改。
- 中枢医疗在独立的零时间事务中使用同一份真实库存；当前未实现维修、充电、制作、拆解、日结算、RunState、存档和 UI。

## Run 中枢医疗

`src/core/run-hub-medical` 在最小 `RunLoadoutSnapshot` 之上只包装玩家条件和每日医疗使用次数，不创建完整 RunState。

- 绷带、止痛药、消毒剂和急救包复用共享的物品级医疗绑定、资格、目标与主效果规划；场景只负责正时间编排，战斗只负责 CTB 编排，中枢只执行配置确认的 `hubSceneTime = 0` 事务。
- 命令必须显式选择 Warehouse、Backpack 或 Quick Slot 中的真实实例；Task Storage 与装备栏不属于可用来源，且不会自动寻找、移动、合并或补充物品。
- 消费一单位堆叠时保留原实例和 ItemState；完全消费时才删除实例、背包摆放或快捷栏占位以及对应 ItemState。
- 消毒剂使用同一份 `DailyMedicalUsage`，场景返回后不会因进入中枢而重置；当前不实现日期推进、日结算、感染进展或自然恢复。
- 每次中枢医疗通过严格命令、冻结完整 Effect 计划、计划重建比对和最终快照一次性提交；零时间确认不调用场景时间、CTB、行动后流血或强制返程。

## 场景时间事务

`src/core/scene` 负责正时间非战斗场景行动的通用事务。具体行动先由调用方计算主要效果和行动完成后的预计返程时间，通用事务再统一处理时间扣除、超时债务、行动后流血、死亡检查和紧急返程。

- 节点、负载、伤势与路线规则由其他模块计算，不在场景事务中推断。
- 预览和正式结算共用同一权威计算逻辑。
- 输入快照不会被修改，事务输出及其嵌套快照在运行时冻结。
- 场景核心不依赖医院内容、随机模块或 UI。

## 负载与移动时间

`src/core/load` 负责根据调用方提供的背包总重量判定负载等级，并计算节点移动和返程共用的时间修正。

- 物品重量求和和背包格子布局不属于本模块。
- 多个百分比倍率使用整数比例相乘，只在最终结果处向上取整一次。
- 轻微挫伤增加移动与返程时间；镇痛生效时暂时抑制该修正，但不移除伤势。
- 本模块只提供三种可携带负载等级；战斗模块结合背包负载与条件模块的伤口修正生成正式逃跑计划。
- 场景事务只接收已经计算完成的返程时间，不反向依赖负载模块。

## 场景节点图与返程路径

`src/core/scene-graph` 负责不可变静态拓扑、调用方显式提供的可通行边，以及采用稳定平局规则的确定性路径。

- 静态图只描述节点和潜在连接；工具、门禁卡、事件及开门资格由其他模块处理。
- 返回路线先合计所选路线的基础时间，再调用负载模块统一应用负载与伤势倍率并最终取整。
- 场景事务只接收最终预计返程时间，不读取节点图或重复应用时间修正。
- 核心图模块不依赖医院内容、随机源、全局可变状态或浏览器能力。

## 物品身份与背包网格

`src/core/inventory` 负责固定物品定义、稳定实例身份，以及不可变的背包几何、摆放操作和重量小计。

- 物品目录不包含随机品质、随机词缀或随机属性，只有 `1×1` 定义可以声明堆叠。
- 背包快照、摆放、占用格和添加、移动、移出结果均不可变并使用稳定实例ID排序。
- 本模块只计算背包重量小计；调用方可以将小计交给负载模块，但不能把它误称为完整总携带重量。
- 装备槽、快捷栏、仓库、耐久、正式医院物品及物品效果由后续模块处理。
- 背包整理本身不进入正时间场景事务。

## 物品状态资源

`src/core/item-state` 负责独立于物理库存的耐久、完整度和电量状态，以及不可变的消耗与恢复结算。

- 资源档案必须与调用方提供的物理物品定义完整、一一对应；未知、遗漏和重复定义都会明确失败。
- 耐久与完整度允许剩余至少 1 点的已承诺行动完成，并在成本超过剩余量时归零；电量不得透支。
- 资源归零只改变状态，不删除物品实例或改变实例身份。
- 资源恢复在正式上限处截断并返回未使用数量，不实现维修材料、工时或具体行为资格。
- 模块不解释攻击、防护、照明、装备槽、消耗品效果或零资源后的临时攻击规则。
- 随身物品状态集合要求背包、装备栏和快捷栏中的每个实例恰好具有一个身份一致、资源类型合法的状态；节点未拾取物品不进入该集合。

## 装备栏与背包转移

`src/core/equipment` 负责武器、防具和实用装备槽、装备资格，以及背包与装备栏之间的不可变转移。

- 装备栏不占背包格，装备栏物品不计入背包负重；负载只根据背包重量小计判断。
- 从背包装备会降低背包占格和背包负重，卸下至背包则可能提高背包负重。
- 资源归零不会自动卸下、移动或删除物品，也不会改变装备资格。
- 装备、卸下和显式交换不自动寻找位置、旋转或替换。
- 装备核心提供物理转移能力，不代表任何游戏上下文都允许换装；调用层负责控制操作资格。
- 快捷栏不属于当前模块。

## 快捷栏与单件转移

`src/core/quick-slot` 负责规则指定数量的快捷栏容器状态，以及背包、装备栏和快捷栏之间的实例唯一性与不可变转移。

- 快捷栏保存数量为1的实际物品实例，不是背包实例引用。
- 从背包堆叠抽取一件时，调用方必须显式提供新的稳定实例ID。
- 快捷栏物品不占背包格，也不计入背包负重。
- 移出或以后使用快捷栏物品后不会从背包自动补充。
- 本模块不执行物品效果、使用时间、CTB、命令编排或UI状态。
- 战斗模块只读取快捷栏中的实际单件实例；战斗消耗会同时清空指定槽位并移除对应 ItemState，不读取或自动补充背包同定义物品。

## 玩家条件状态

`src/core/condition` 负责生命、流血、未处理与已处理开放伤口、轻微挫伤和镇痛状态。

- 开放伤口使用带稳定ID、类型和处理状态的记录作为唯一事实源；未处理、已处理和总数均由选择器派生。
- 待处理感染暴露与感染进展分离；当前只累计和减少暴露，不执行感染进展换算。
- 各状态相互独立；具体医疗行为以后按正式规则组合多个基础操作。
- 消毒剂每日使用次数是独立的日级状态；场景事务只显式携带和更新它，创建场景时必须传入，不能把创建场景隐式视为开始新的一日。
- 场景行动后的流血生命损失仍由 `src/core/scene` 统一结算，条件模块不重复扣除。
- 轻微挫伤只向旅行时间模块提供修正是否生效的布尔状态，不复制移动倍率。
- 逃跑伤口加时只计算未处理开放伤口修正，不包含基础逃跑 CTB。
- 条件模块不执行医疗物品消耗、日结算、感染进展或 Run 终止；从生命0恢复的纯数值操作不代表终局后允许复活。

## 当前日中枢与生存事实

`src/core/current-day-hub` 将返回结算后的整备库存、玩家条件、Run情报、日级状态、世界威胁、饱食和返回记录聚合为严格的当前日中枢稳定状态；它不是完整 `RunState`。

- `src/core/domain/run-phase-continuity.ts` 复用唯一的 `RunIdentity`，只记录当前日与刚完成场景实例；Return输入、Return结果、跨场景事实和CurrentDayHub必须逐字段保持同一连续性，并绑定依赖实际加载的规则版本。
- `createCurrentDayHubSnapshotFromReturn` 统一承接 `RunReturnSnapshot` 和一份严格的跨场景日级／威胁事实，不要求上层手工拼接多个快照。
- `src/core/world-threat` 只表达可替换的威胁定义、非负进展和派生阶段；医院感染阶段名称只存在于医院内容层。
- `src/core/satiety` 负责有上限的饱食事实与恢复；`src/core/daily-state` 额外记录威胁抑制次数／抑制量和剩余维护工时。
- 整备和中枢医疗通过适配器更新同类中枢快照，未参与的情报、威胁、饱食、日级事实和返回记录保持原样。
- 中枢医疗与中枢生存消耗品共同复用 `src/core/hub-inventory` 的真实实例消费边界；只接受仓库、背包或快捷栏的显式来源，不自动寻找、合并或补充。
- 当前实现感染抑制剂和口粮的零时间中枢使用；未实现日结算、感染暴露换算、威胁推进、日期推进、自然恢复、维护行为、完整RunState或UI。
- 通用世界威胁快照可以表达终末进展，但终末威胁不再是合法的活动CurrentDayHub状态；Run失败及终止协调仍由后续生命周期实现。

## 场景探索与移动命令

`src/core/scene-exploration` 负责最小场景探索快照和第一条完整移动命令编排。

- 移动命令以稳定边ID组合场景图、背包负载、挫伤与镇痛、场景时间、行动后流血和确定性返程。
- 负载只读取背包，不读取装备栏或快捷栏。
- 场景探索快照会将背包宽高绑定到当前版本化规则配置；通用背包核心仍可处理其他合法尺寸。初始构造入口可以显式补齐警觉、节点地面物品和场景战斗初态，正式恢复入口则要求全部字段精确存在，不会因字段遗漏静默重置隐藏状态。
- 主要移动效果先发生，随后才结算行动后流血；死亡优先于安全返回和强制返程。
- Effect是有序、结构化的规则结果，不是UI文案。
- 移动命令只接受精确的单字段 `edgeId` 结构，并由唯一纯计划生成器产生冻结的完整Effect计划；Effect应用器会从起始快照重新生成并逐项比对完整计划，通过后才由内部提交路径依序生成最终快照。
- 同一Effect计划可以在等价起始快照上确定性回放；这不代表已经实现事件溯源存档。
- 主要搜索命令要求玩家显式选择使用已装备照明或无灯搜索；不会自动寻找、装备或使用背包与快捷栏物品。
- 搜索时间直接读取版本化的20/30配置，不应用负载、挫伤或镇痛修正；行动后返程仍按背包负载、挫伤和镇痛状态计算。
- 手电筒照明通过 `item-resource-consumed` Effect 消费，预定结果通过 `scene-main-search-revealed` Effect 揭示；搜索不会自动拾取。
- 节点物品拾取必须显式指定已揭示实例、数量、背包坐标和旋转，不自动选择、放置、旋转、合并、装备或进入快捷栏。
- 完整拾取保持原实例身份；部分拾取只允许无资源状态的可堆叠物品，并要求调用方提供新的稳定实例ID。
- 拾取不消耗场景时间，不执行流血或强制返程；单个原子 `scene-item-picked-up` Effect 同时更新节点物品、背包和随身资源状态。
- 工作人员通道实时读取背包中的实体门禁卡，不消耗卡，也不会把权限边持久写入场景状态；防火门打开则持久写入通行边。
- 场景警觉是 `unalerted/alerted` 单级事实；进入仍有感染护工的隔离走廊时，它决定首次敌人行动时间，不累计噪声。
- 当前移动、主要搜索、显式拾取、防火门、场景非战斗医疗与场景战斗命令采用场景Effect应用器生成最终快照；战斗内部仍由独立Combat Effects应用器提交。场景医疗通过真实背包或快捷栏物品实例使用绷带、止痛药、消毒剂和急救包；电梯中枢医疗由独立零时间 Run 库存事务编排。
- 场景任务事件使用严格目录、状态和前置遭遇绑定；已完成事件必须保留其来源情报，目录与遭遇依赖会在恢复和命令边界校验。确定性污染、稳定任务物实例和完整 Effect 计划回放均通过同一场景提交路径完成。
- 当前没有通用事件总线、发布订阅系统或全局Effect历史。
- 场景探索快照不是完整 RunState。

## 场景障碍完整性边界

`src/core/scene-obstacle` 负责严格校验障碍内容目录，并为一个障碍选项生成唯一的确定性主要结果计划。命令生成和 Effect 回放共同使用该计划，不分别维护装备资格、资源成本、正式时间、警觉、风险或产物规则。

- 障碍应用器校验完整主要结果的数量、内容和顺序，不只校验单个 Effect。
- 强行撞门始终产生可审计的确定性风险 Effect；回放必须提供 Run seed，并重新验证算法版本、命名子流、抽取序号、随机值、风险阈值、防护使用和挫伤结果。
- 工具箱产物的定义、数量、初始资源状态、当前节点、来源和稳定实例 ID 均由共享计划锁定。
- 障碍目录拒绝未知字段和未知判别类型，并交叉校验受控边端点、物品存在性、装备槽资格、资源档案、堆叠上限和产物初始状态。
- 本边界不负责感染护工战斗或 CTB 时间轴；战斗由 `src/core/combat` 独立负责。

## 单敌人 CTB 战斗

`src/core/combat` 负责独立于场景快照的单敌人CTB战斗事实、玩家行动计划、敌人自动行动、确定性风险和战斗终局。

- 战斗快照是严格恢复边界：顶层字段、终局状态、正式背包尺寸、快捷栏数量和玩家条件都会按绑定的版本配置重新校验；玩家死亡与敌人同时失能时只能恢复为战败。
- 玩家条件拒绝旧伤口计数字段和未知字段；类型化伤口由共享构造器校验，所有派生计数只读取同一伤口列表。
- 敌人当前意图、下一循环位置与已结算行动次数构成固定循环不变量；首次遭遇只接受满生命、零行动且初始意图未改变的干净状态，重入不重新生成这些事实。
- 战斗内容绑定在所有公共战斗入口执行运行时验证；敌人、金属管、厚实外套及其装备资格、资源类型和最大资源必须与版本配置一致。
- 正式玩家决策点与终局快照不保留临时防御；防御的内部 Effect 状态同样使用严格结构，并在战斗结束前清除。
- 玩家与敌人同一CTB时间点时玩家优先；一次命令只推进到下一玩家决策点或终局。
- 支持金属管基础攻击、蓄力击打、通用防御、临时攻击和锁定完成时间的逃跑，以及感染护工抓挠、扑咬和固定交替意图。
- 风险使用Run seed、场景实例、敌人实例、行动计数、行动ID和风险用途派生的独立子流。
- CTB Effect 的 `before/after` 使用计划内的实际当前时间游标形成连续链；Combat Effects仍是唯一状态提交路径，应用器会重建并核对完整正式计划后再生成最终快照。
- 战斗医疗命令显式指定快捷栏索引；存在未处理开放伤口时，绷带还必须显式指定伤口ID。绷带先恢复生命、停止流血并处理指定伤口，因此本次不再承受行动后流血；止痛药不止血，流血时仍在主效果提交后损失1生命。
- 绷带与止痛药的战斗CTB均读取版本化配置；使用后槽位保持为空且不自动补充。止痛药不叠加，并只在已确认伤势资格成立时提供命令。
- 终局CTB按版本配置一次换算场景时间；敌人、玩家、装备资源和探索战斗用量通过场景Effect写回，胜利或逃跑超时会接入既有强制返程。
- 电梯中枢医疗由独立 Run 库存事务实现；当前未实现战利品、第二敌人或多目标战斗。

## 场景战斗遭遇

`src/core/scene-combat` 负责严格遭遇目录、稳定敌人实例身份和场景级遭遇状态；`src/core/scene-exploration` 负责编排移动触发、战斗推进、终局时间与状态写回。

- 场景状态增加 `combat`；移动Effect在行动时间与流血结算后原子触发仍存在的节点遭遇。
- 场景状态为 `combat` 时，活跃 CombatSnapshot 与顶层随身事实必须完全一致，非战斗命令会被阻断。
- 活跃遭遇同时记录 `returnNodeId` 与本次真实移动的 `entryEdgeId`；恢复时会验证入口边确实从来源节点通往遭遇节点且在当前快照中获准通行。
- 普通 `active` 状态不能停留在仍有未击败自动遭遇的节点；战斗胜利后的已击败节点和玩家死亡时保留的战斗节点按各自终局事实恢复。
- 单次战斗中的行动不逐次扣场景时间；胜败或逃跑终局按实际CTB一次换算。
- 逃跑准备在选择时锁定，等点时逃跑完成优先；准备期间的新伤口与流血会保留但不追溯延长本次完成时间。
- 首次遭遇读取场景警觉；逃跑重入固定使用重入时序，并保留敌人生命、意图循环与行动计数。
- 胜利或逃跑使场景时间归零时使用终局后的真实状态立即强制返程；战败保持战斗节点并直接写入死亡。
- 玩家可见战斗投影公开相对敌人生命阶段、当前意图、具体伤口ID、快捷栏状态和合法具体命令，不泄露精确敌人生命、种子、风险抽取或未来循环；旧的合法动作种类由具体命令派生。
